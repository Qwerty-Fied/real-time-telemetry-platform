# Grafana Dashboards

이 디렉토리는 Grafana 대시보드 JSON 파일을 저장하기 위한 공간입니다.

대시보드를 Grafana UI에서 생성한 후 다음 절차로 저장합니다.

Grafana → Dashboard → Settings → JSON Model → Export

export한 JSON 파일을 이 디렉토리에 저장합니다.

예시:

telemetry-dashboard.json

저장된 대시보드는 Grafana provisioning 기능을 통해 자동 로드할 수 있습니다.
